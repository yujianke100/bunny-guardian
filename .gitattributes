# 一律用 LF 存进仓库。
#
# 为什么必须管：deploy/*.sh 与 systemd 单元要在 Linux 服务器上跑，
# 带 CRLF 时 `sh deploy/install.sh` 会直接 `set: Illegal option -` 跑不起来
# （踩过一次：在 Windows 上编辑后传上去的检出全是 CRLF）。
* text=auto eol=lf

*.sh   text eol=lf
*.timer text eol=lf
*.service text eol=lf
*.py   text eol=lf
*.md   text eol=lf
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.json text eol=lf
*.sql  text eol=lf
*.txt  text eol=lf

# 二进制：不要做任何换行或编码转换
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.gif  binary
*.pdf  binary
*.woff  binary
*.woff2 binary
