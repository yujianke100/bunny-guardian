---
title: CLAUDE
tags: [claude, agent-guidance]
---

# CLAUDE.md — 数据仓库维护指引

本仓库是**个人健康数据**，由诊籍（Zhenji）系统读写。系统代码在另一个仓库里。

## 铁律

1. **隐私第一**：本仓库必须保持私有；不要把它推送到公开远端，也不要把内容粘贴到外部服务。
2. **不诊断、不处方、不给剂量**：可以解释指标含义、列出有来源的判定标准、给出就医时机建议，
   但不能输出诊断结论或用药剂量。
3. **每条医学事实必须标注来源**：格式 `[来源: <URL>]`；查不到来源的写「（待核实）」。
   区分「指南原文」与「（一般性解释）」。
4. **区分查到值与推断值**：统计预测（如下次经期）必须标注为推断并给出区间。
5. **不要手工改写由系统生成的文档结构**：`docs/经期记录/`、`docs/疾病档案/`、`docs/就医记录/`
   下的文件由网页「导出」生成；要改数据请在网页上改后重新导出。

## 目录职责

| 目录 | 内容 | 谁能写 |
|---|---|---|
| `docs/经期记录/` | 周期、经量、症状记录与统计 | 系统导出 |
| `docs/疾病档案/` | 疾病、病程事件、检查结果 | 系统导出 |
| `docs/就医记录/` | 就诊记录 | 系统导出 |
| `docs/医学知识/` | 带来源的医学知识文档 | 人工/助手整理 |

## 文档写法

- YAML frontmatter：`title`、`tags`（数组）、`aliases`（数组）、`last_updated`；
  医学知识文档再加 `source`。
- 结构建议：概述 → 判定标准/要点 → 就医建议 → 来源列表 → 免责声明。
- 交叉引用 `[[文件名]]`；新增文件后更新 `docs/INDEX.md`。

## 助手在数据侧能做什么

系统的在线助手只能**读**本仓库（检索、引用），所有写入都要经过人工批准；
它也可以直接维护 `docs/医学知识/` 下的文档（这部分是通用医学知识，不是个人记录）。
